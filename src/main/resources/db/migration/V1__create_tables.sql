CREATE TABLE IF NOT EXISTS awards (id INT AUTO_INCREMENT PRIMARY KEY, year_string VARCHAR(20) NOT NULL, category VARCHAR(255) NOT NULL, nominee TEXT NOT NULL,additional_info TEXT,won BOOLEAN);
CREATE TABLE IF NOT EXISTS movies (id INT AUTO_INCREMENT PRIMARY KEY, title VARCHAR(100) NOT NULL, year INT NOT NULL, num_ratings INT NOT NULL, av_rating FLOAT);